# Server
PORT=3000

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=hospital
DB_PASSWORD=hospital_secret
DB_NAME=hospital_db

# JWT
JWT_SECRET=your-super-secret-key-change-this
JWT_EXPIRY_HOURS=72

# CORS
ALLOWED_ORIGINS=*
